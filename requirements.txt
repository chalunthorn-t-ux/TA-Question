# ---- Core web ----
fastapi>=0.115
uvicorn[standard]>=0.34
jinja2>=3.1
python-multipart>=0.0.20
pydantic>=2.10
python-dotenv>=1.0
httpx>=0.28
itsdangerous>=2.2         # เซ็นคุกกี้ session (SessionMiddleware)

# ---- Document loaders ----
pypdf>=5.1              # PDF
python-docx>=1.1        # Word (.docx)
openpyxl>=3.1           # Excel (.xlsx / .xlsm)
xlrd>=2.0               # Excel รุ่นเก่า (.xls) — ไม่บังคับ

# ---- ที่เก็บข้อมูลถาวร (production) ----
psycopg[binary]>=3.2    # PostgreSQL (Neon) — เก็บบัญชีผู้ใช้แทนไฟล์ JSON

# ---- Vector math ----
numpy>=2.1
